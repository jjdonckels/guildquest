//i used enum instead of strings bc to avoid typos and keeps results consistent
public enum GameResult{
    WIN, //for solo wins
    LOSS, //solo loss
    COOP_SUCCESS, // duo wins
    COOP_FAIL //duo loses
}

//ex)  //EscortGame | Seed: 12345 | Players: [Player 1, Player 2] | Result: COOP_SUCCESS | Turns: 12

